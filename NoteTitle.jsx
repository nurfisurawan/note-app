import React from 'react';

function NoteTitle({title}) {
    return (
        <div className='note-title'>
            <h3>{title}</h3>
        </div>
    );
    
}

export default NoteTitle;
